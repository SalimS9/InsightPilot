# InsightPilot

Small web app with `Index.html`, `script.js`, and `style.css`.

This repository contains the project files as they were when uploaded; no changes were made.
Open `Index.html` in a browser to run the app.
